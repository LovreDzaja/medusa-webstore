import React from 'react';

import ProductList from "@/components/ProductList";

const Home: React.FC = () => {
  return (
    <div className="relative">
      <ProductList />
    </div>
  );
};

export default Home;
